@model IEnumerable<WebArtSampler.Models.PatternStyle>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StyleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StyleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PatternStyleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PatternStyleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PatternStyleID })
        </td>
    </tr>
}

</table>
